create or replace package test_k_dato is

  --%suite(Tests unitarios del paquete k_dato)
  --%tags(package)

  --%test(Guarda un dato adicional de tipo string)
  procedure p_guardar_dato_uso_basico;

end;
/
